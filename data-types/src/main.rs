fn main() {
    // PRIMITIVE TYPES
    
    // INTS
    //unsigned => only positive numbers 
    //u8 => 8bits u16, u32, u64, u128
    let unsigned: u8 = 10;

    //signed => positive and negative numbers
    let signed: i32 = -12;

    //float => positive or negative decimals
    //it must be at least 32
    let fload_num: f32 = -2.3; 

    //BOOL
    let is_true: bool = true;

    //CHAR
    let one_char_only: char = 'a';
}
